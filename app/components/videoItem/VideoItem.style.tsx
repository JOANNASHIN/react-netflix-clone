import { css } from '@emotion/react';

const color = {
  black: '#111',
  red: '#ff0000',
  white: '#fff',
};

const styles = css`
  .video-item {
    position: relative;
    cursor: pointer;
    // test
    width: 300px;
  }
`;

export default styles;
